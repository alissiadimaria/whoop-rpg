services:
  - type: web
    name: whoop-rpg-api
    env: python
    buildCommand: pip install fastapi uvicorn anthropic python-dotenv pydantic
    startCommand: uvicorn src.api:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: ANTHROPIC_API_KEY
        sync: false
